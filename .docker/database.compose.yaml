volumes:
  postgres-data:
services:
  postgres:
    image: docker.io/postgres:18
    container_name: tablespoon-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: tablespoon
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql
